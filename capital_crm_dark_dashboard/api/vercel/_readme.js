/*
  Stubbed API for demo. In production, replace with real DB connection (MYSQL_URL).
  Endpoints provided:
  - POST /api/login  -> { email, password } returns token for demo
  - GET  /api/reports/summary -> returns totals for dashboard
*/
